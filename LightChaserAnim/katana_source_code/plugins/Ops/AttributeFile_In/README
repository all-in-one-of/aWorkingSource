AttributeFile_In Op
===================

The AttributeFile_In Op implements the same features as the AttributeFile AMP.
It parses a file with attribute information associated with scene graph
locations and adds those attributes to those locations.

Environment Variables
---------------------

KATANA_HOME   The Katana installation directory at compile-time
TINYXML_HOME  The installation directory of TinyXML

Building
--------

1. Ensure the environment variables listed above have been set.
2. Run 'make'.
3. Run 'make install'.
